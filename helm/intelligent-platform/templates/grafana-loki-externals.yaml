{{/* 
This chart previously created in-namespace Services pointing at ai-healing-system Grafana/Loki.
Those selector-based services do not produce endpoints across namespaces, so Traefik cannot route to /grafana and /loki.
We now expose Grafana/Loki via an in-namespace nginx proxy (see:
- grafana-loki-proxy-deployment.yaml
- grafana-loki-proxy-configmap.yaml
).  

No changes are made to ai-healing-system monitoring components.
*/}}

{{- /* Intentionally empty (disabled) */ -}}

